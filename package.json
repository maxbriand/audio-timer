{
  "name": "audio-timer",
  "version": "1.0.0",
  "private": true,
  "description": "Offline audio player with a custom sleep timer that saves the exact moment it stops.",
  "scripts": {
    "build": "node scripts/build-www.mjs",
    "sync": "npm run build && npx cap sync android",
    "apk": "npm run sync && node scripts/build-apk.mjs"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
