<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Watches the accelerometer while a track is selected, so a shake can act with the
             screen off: resume when the audio is paused, or skip to the next chapter while it
             plays. Android 14+ demands a declared type; none of the named ones fit a bedside
             sensor, hence specialUse. -->
        <service
            android:name=".ShakeService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Listens to the accelerometer while audio is selected so shaking the phone can resume playback or skip to the next track" />
        </service>

        <!-- The fatigue check, 45 minutes after the day-mode switch. The receiver fires the
             ring; AlarmActivity is the alarm screen itself, allowed over the lock screen;
             BootReceiver re-arms a pending check that a reboot would otherwise drop. -->
        <receiver
            android:name=".FatigueAlarmReceiver"
            android:exported="false" />
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <activity
            android:name=".AlarmActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:launchMode="singleInstance" />

        <!-- The daily melatonin reminder (bedtime − 5 h, set in ⚙). Same shape as the
             fatigue alarm; its notification is un-swipeable and only "Taken ✓" ends it. -->
        <receiver
            android:name=".MelatoninReceiver"
            android:exported="false" />
        <activity
            android:name=".MelatoninActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:launchMode="singleInstance" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- The fatigue alarm. USE_EXACT_ALARM (13+) is the alarm-clock permission — granted by
         install, not revocable, which is what "exactly like the classic alarm" requires;
         SCHEDULE_EXACT_ALARM covers 12/12L, where USE_EXACT_ALARM does not exist yet. -->
    <!-- Doze ignores app wake locks, so the shake watch dies on a phone lying still with
         the screen off; the exemption (asked once, when a timer is armed) is what keeps
         the sensor alive through the night. Sideloaded app: Play's policy on this
         permission does not apply. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
</manifest>
